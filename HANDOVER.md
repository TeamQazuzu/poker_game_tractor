# 拖拉机游戏 — 项目交接文档

> 仓库地址：https://github.com/TeamQazuzu/poker_game_tractor
> 最后更新：2026-07-30（v2 - AI策略增强版）

## 项目概述

这是一个纯前端的单机拖拉机（升级）卡牌游戏，使用 HTML + CSS + JavaScript 实现，无需后端、无需构建工具，浏览器直接打开即可运行。玩家与三个 AI 对手进行 4 人对局，支持完整的拖拉机规则体系，包括叫主/反主、埋底、抠底、甩牌等高级规则。

## 快速开始

```bash
git clone https://github.com/TeamQazuzu/poker_game_tractor.git
cd poker_game_tractor
```

用浏览器打开 `index.html` 即可。无需安装任何依赖，无需启动服务器。

## 目录结构

```
tractor-game/
├── index.html          # 游戏主页面，定义 DOM 结构和弹窗
├── css/
│   └── style.css       # 全部样式（绿色牌桌、卡牌、弹窗、动画）
└── js/
    ├── game.js          # 游戏核心逻辑（规则引擎、计分、状态管理）
    ├── ai.js            # AI 决策引擎（出牌、埋底、叫主、跟牌策略）
    └── ui.js            # UI 交互层（渲染、动画、游戏流程控制）
```

三个 JS 文件的加载顺序在 `index.html` 底部固定为 `game.js → ai.js → ui.js`，因为后两者依赖前者定义的全局常量和函数。

## 架构设计

### 三层分离

| 层 | 文件 | 职责 |
|---|---|---|
| **规则引擎** | `game.js` | 纯逻辑，不操作 DOM。定义常量、牌型识别、合法性校验、胜负判定、计分升级 |
| **AI 引擎** | `ai.js` | 依赖 `game.js` 的函数。根据手牌和桌面状态做出叫主、埋底、出牌决策 |
| **UI 控制** | `ui.js` | 依赖 `game.js` 和 `ai.js`。管理游戏流程、渲染界面、处理用户交互 |

### 核心数据流

```
用户操作 → ui.js 调用 game.js 方法 → game.js 更新状态 → ui.js 读取状态并渲染
                                    ↓
                            ai.js 读取状态 → 做出决策 → ui.js 执行 AI 出牌
```

### 全局状态对象

`GameState` 类（`game.js` 第 920 行）是整个游戏的单一状态源：

```javascript
class GameState {
    reset() {
        this.players = { bottom: [], left: [], top: [], right: [] }; // 四家手牌
        this.kitty = [];                // 底牌（8张）
        this.trumpSuit = null;          // 主花色，null = 无主
        this.level = '2';               // 当前打的级
        this.dealer = null;             // 庄家位置
        this.trickScores = { teamA: 0, teamB: 0 };  // 本局累积得分
        this.lastTrickInfo = null;      // 最后一轮牌型（供抠底倍数计算）
        this.playedCardsHistory = [];   // 本局所有已出过的牌（供甩牌验证）
        this.phase = 'idle';            // idle → dealing → bidding → burying → playing → ended
        this.bidState = { ... };        // 叫主状态
        this.teamLevels = { teamA: '2', teamB: '2' }; // 两队级别
    }
}
```

游戏阶段（`phase`）流转：`idle → dealing（发牌）→ bidding（叫主）→ burying（埋底）→ playing（出牌）→ ended（结束）→ idle（下一局）`

## game.js 核心函数索引

### 牌型与比较

| 函数 | 行号 | 说明 |
|---|---|---|
| `isTrump(card, trumpSuit, level)` | 249 | 判断是否主牌（王/级牌/主花色） |
| `getCardValue(card, trumpSuit, level)` | 301 | 获取牌的点数价值（排序用），主牌 200-215，副牌 0-12 |
| `getCardPattern(cards, ...)` | 400 | 识别牌型：single / pair / tractor / throw / invalid |
| `isThrowValid(cards, ...)` | 493 | 验证甩牌合法性：所有牌须为该门副牌剩余最大的 |
| `isCardPair(card1, card2, ...)` | 563 | 判断两张牌是否构成对子 |
| `compareCards(...)` | 340 | 比较两张牌大小（考虑跟牌花色和主牌杀） |

### 游戏规则

| 函数 | 行号 | 说明 |
|---|---|---|
| `isValidPlay(...)` | 586 | 出牌合法性校验（跟牌规则、对子/拖拉机/甩牌跟牌） |
| `getTrickWinner(...)` | 792 | 判断一轮的赢家（处理杀牌、甩牌杀牌逻辑） |
| `getCardScore(cards)` | 865 | 计算分数（5=5分，10=10分，K=10分） |
| `calculateUpgrade(score, isDealerTeam)` | 893 | 计算升级级数 |
| `getNextLevel(current, upgrade)` | 912 | 获取升级后的级别 |

### GameState 方法

| 方法 | 行号 | 说明 |
|---|---|---|
| `deal()` | 961 | 洗牌并发牌（25/25/25/25 + 8底牌） |
| `makeBid(player, bidInfo)` | 1052 | 执行叫主/反主 |
| `buryCards(cards)` | 1084 | 庄家埋底牌 |
| `playCard(position, cards)` | 1104 | 执行出牌 |
| `endTrick()` | 1126 | 结束一轮，计算得分，保存牌型信息 |
| `endRound()` | 1163 | 结束一局，计算抠底、升级、轮换庄家 |
| `prepareNextRound()` | 1274 | 重置局内状态，准备下一局 |

## ai.js 核心函数索引

### 记忆系统

AI 维护一个 `memory` 对象，在每轮出牌后更新：

```javascript
this.memory = {
    playedCards: [],     // 所有已出过的牌（用于甩牌验证、判断大牌是否还在）
    suitVoid: {          // 各玩家断掉的花色（用于预判杀牌）
        left: new Set(), top: new Set(), right: new Set(), bottom: new Set()
    }
};
```

### 决策入口

| 方法 | 行号 | 说明 |
|---|---|---|
| `decideBid(hand, level, currentBid)` | 236 | 叫主决策 |
| `decideBury(hand, trumpSuit, level)` | 344 | 埋底决策（评估主牌强度、保护分牌） |
| `decidePlay(hand, gameState, trickCards)` | 554 | 出牌决策（首家/跟牌分流） |

### 出牌策略

| 方法 | 行号 | 说明 |
|---|---|---|
| `_leadPlay(...)` | 569 | 首家出牌（区分庄家/闲家、尾盘/中盘） |
| `_endGameLeadPlay(...)` | 628 | 尾盘首家策略（大王留最后一轮、拖拉机/对子控制） |
| `_leadAsDealer(...)` | 773 | 庄家队首家（出副牌A让队友跑分） |
| `_leadAsAttacker(...)` | 895 | 闲家队首家（出小牌消耗、长套副牌） |
| `_findThrowOpportunity(...)` | 1131 | 检测甩牌机会 |
| `_followPlay(...)` | 1219 | 跟牌策略入口 |
| `_followTrump(...)` | 1269 | 跟主牌 |
| `_followSuit(...)` | 1388 | 跟副牌花色 |
| `_followNoSuit(...)` | 1807 | 断门后的杀牌/垫牌 |
| `_followThrow(...)` | 1946 | 跟甩牌（杀牌需断门+全主牌+对子数达标） |

### 尾盘策略核心

AI 的尾盘策略基于一个核心原则——"先出的大王大"：

- 大王作为首家出牌时，跟随者的大王无法杀牌（同等级先出为大）
- 因此大王的最大价值在于最后一轮主动出
- 倒数第二轮用较大非大王牌抢出牌权，最后一轮再主动出大王确保赢

### 埋底策略

`decideBury` 方法（第 344 行）综合评估以下因素决定埋哪些牌：

- 主牌强度评估（`_evaluateTrumpStrength`）：大王+12分，小王+8分，级牌+6/3分
- 底牌保护信心：有大王时可放心埋分牌，无大王时收紧门槛
- 断门策略：弱主牌时避免埋断门花色，保留杀牌能力

## 游戏规则详解

### 基本设置

- 2 副牌共 108 张，4 人各发 25 张，8 张底牌由庄家埋底
- 分数牌：5（5分）、10（10分）、K（10分），总计 200 分
- 所有分数都是 5 的倍数，不存在 1-4、36-39、76-79 等分值

### 牌型定义

| 牌型 | 张数 | 说明 |
|---|---|---|
| 单张 | 1 | 任意一张牌 |
| 对子 | 2 | 同花色同点数不同副牌，或同等级的两张王 |
| 拖拉机 | 4+（偶数） | 相邻对子组成的连对，如 4455、KKAA、主级对小王对 |
| 甩牌 | 2+ | 该门副牌中剩余最大的牌，不要求是对子或拖拉机 |

### 主牌排序

以打 5 红桃主为例（`getCardValue` 函数）：

```
副牌（0-12）：  2 < 3 < 4 < 6 < 7 < 8 < 9 < 10 < J < Q < K < A
                （5被提取为主牌，不在副牌序列）

主牌（200-215）：主花色普通牌(200-211) < 副级牌(212) < 主级牌(213) < 小王(214) < 大王(215)
```

级牌被提取后，相邻牌的 value 差为 1，保证拖拉机连续性（如打5时红桃4和红桃6相邻）。

### 跟牌规则

1. 出牌数量必须与首家一致
2. 有首家花色的牌必须跟该花色（有多少跟多少，不够的垫其他牌）
3. 对子跟牌：有对子必须出对子，没对子可出单牌
4. 拖拉机跟牌：有拖拉机跟拖拉机，没拖拉机跟对子，再没有出单牌

### 杀牌规则

- 必须断门（手中无该花色副牌）才能用主牌杀
- 杀对子需出主牌对子
- 杀拖拉机需出主牌拖拉机
- 杀甩牌需满足：全主牌 + 张数相同 + 对子数不少于甩牌中的对子数

### 叫主/反主

| 叫主类型 | power | 说明 |
|---|---|---|
| 单张级牌 | 1 | 用手中的一张级牌叫主花色（仅首轮可叫） |
| 对级牌 | 2 | 反主，用同花色对级牌改变主花色 |
| 对小王 | 3 | 反主为无主 |
| 对大王 | 4 | 反主为无主（最高优先级） |

### 抠底倍数

最后一轮的赢家获得底牌分数。闲家赢时按牌型翻倍：

| 最后一轮牌型 | 倍数 | 示例 |
|---|---|---|
| 单张 | ×2 | 底牌20分 → 40分 |
| 对子 | ×4 | 底牌20分 → 80分 |
| 拖拉机 | ×8 | 底牌20分 → 160分 |
| 甩牌 | ×(张数×2) | 甩5张 → ×10，底牌20分 → 200分 |

庄家方赢最后一轮时，底牌分数原样归还，不翻倍。

### 计分与升级

总分 = 走牌累积得分 + 抠底翻倍得分

**庄家守擂成功**（闲家得分 < 80）：

| 闲家得分 | 庄家升级 | 庄家轮换 |
|---|---|---|
| 0 | +3级 | 队内对换 |
| 5-35 | +2级 | 队内对换 |
| 40-75 | +1级 | 队内对换 |

**闲家上台**（闲家得分 ≥ 80）：

| 闲家得分 | 闲家升级 | 庄家转移 |
|---|---|---|
| 80-115 | +1级 | 转移到闲家方 |
| 120-155 | +2级 | 转移到闲家方 |
| 160-195 | +3级 | 转移到闲家方 |
| 每+40分 | +1级 | 转移到闲家方 |

公式：`upgrade = floor((score - 76) / 40) + 1`

庄家守擂时下一把在庄家方队内轮换；闲家上台时庄家转移到闲家方（按 PLAYERS 顺序取下家）。

## 测试

测试文件位于 `/data/user/work/`（不在仓库内，属开发辅助文件）：

| 文件 | 测试内容 | 用例数 |
|---|---|---|
| `test_scoring.js` | 计分与升级规则（抠底倍数、升级级数、庄家轮换） | 14 组 |
| `test_ai_strategy.js` | AI 策略（尾盘大王、中盘保守、预判杀牌、埋底） | 12 组 |
| `test_integration.js` | 完整对局集成测试（30 局模拟） | 30 局 |

运行方式：

```bash
cd /data/user/work
node test_scoring.js
node test_ai_strategy.js
node test_integration.js
```

测试通过 VM 沙箱加载 `game.js` 和 `ai.js`，无需浏览器环境。

## AI 策略增强（v2）

### 增强概览

本次更新在原有 AI 基础上，实现了以下高级策略：

1. **对子比较Bug修复** — 修复了首家出对子、跟牌者无对子垫两张单牌时，单牌误判赢牌的Bug
2. **强势主牌+断门策略** — 基于主牌强度评估的断门杀牌决策
3. **反无主策略** — 避免在主牌强势时叫无主，仅在副牌强势+主牌弱时叫无主
4. **开局技巧** — 庄家走小主牌让队友上手、队友判断断门送牌权、对手阻止队友上手
5. **中盘拦截策略** — 队友用中级牌拦截，防止最后一家跑分
6. **A对拆牌策略** — AI出副牌A对时50%概率拆单张、50%概率出对子
7. **UI增强** — 逐张发牌动画、叫主暂停、底牌跟随庄家、亮主标记持久显示

### 阶段判定

AI 根据剩余手牌数判断游戏阶段，不同阶段采用不同策略：

| 阶段 | 手牌数 | 策略重点 |
|---|---|---|
| 开局 | ≥18张 | 出牌权争夺，庄家走小主牌让队友上手 |
| 中盘 | 7~17张 | 拦截策略，防止对手跑分 |
| 尾盘 | ≤6张 | 保底/抠底，大王留最后一轮 |

### 开局技巧逻辑

**庄家AI（走完必胜牌后）：**
- 走完副牌A、K等必胜牌后，若没走过大对子测试（不知道外面是否有对子），走小单主牌让队友上手
- 避免冒险走小对子（被杀风险高）
- 新增方法：`_findBestSmallTrump()`、`_hasTestedBigPairInAnySuit()`

**队友AI（开局抢出牌权）：**
- 庄家走小主牌时，队友主动用稍大主牌压过庄家，拿下出牌权
- 上手后检查庄家是否已显示断门，若是则走该花色单张让庄家杀牌，送回出牌权
- 新增方法：`_leadDealerVoidSuit()`

**对手AI（阻止队友上手）：**
- 庄家走小主牌时，对手积极抢出牌权（非王主牌），阻止庄家队友上手
- 开局阶段不出王来抢小主牌（太浪费）

### 中盘拦截策略

**触发条件**（`_shouldIntercept()`）：
- 中盘阶段（手牌7~17张）
- 桌上无分（trickScore === 0）
- 非最后一家（后面还有玩家可能跑分）
- 队友出小牌赢（不是A/K等大牌）

**副牌拦截**（`_findInterceptCard()`）：
- 优先用J/Q拦截（最理想的中级牌）
- 其次用7/8/9等中级非分牌
- 不用A（中盘AK大概率走完了）、不用K（是分牌）、不用分牌（5/10）

**主牌拦截**（`_findInterceptTrump()`）：
- 优先用单张主牌A（最有价值的拦截牌）
- 其次用主级牌单张
- 不轻易拆A对或级牌对（保留用于抠底/毙杀）
- 最后考虑用小王（大王太珍贵，留给尾盘）

**拦截适用范围**：拦截逻辑在 `iAmWinning` 分支中实现，适用于所有AI（无论庄家队还是闲家队），只要自己的队友出小牌赢且处于中盘，都会执行拦截。

### A对拆牌策略

在 `_decideLeadPair()` 中，AI拿到副牌A对时：
- 50%概率出对子 — 逼对手对子现身
- 50%概率拆单张出 — 利于队友跑10跑5

两种策略同等概率随机选择，增加AI不可预测性。

### 反无主策略

在 `decideBidWithCounter()` 中，AI评估主牌和副牌强度：
- **主牌评分**：大王+4，小王+3，级牌+2
- **副牌评分**：A=3，K=2，Q=1.5，J=1，10=0.5，对子+2
- **叫无主条件**：
  - 庄家型：主牌分≤8 且 副牌分≥12
  - 捣乱型：主牌分≤5 且 有人已叫主
- **不叫无主**：主牌分≥12（强势主牌）

### UI增强

| 功能 | 实现位置 | 说明 |
|---|---|---|
| 逐张发牌 | `ui.js` `startAnimatedDealing()` | 0.1秒间隔逐张发牌 |
| 叫主暂停 | `ui.js` `dealNextCardAnimated()` | 玩家可叫主时暂停5秒 |
| 底牌跟随庄家 | `ui.js` `updateDeckPosition()` | 底牌显示在庄家旁边 |
| 亮主标记 | `ui.js` `updateTrumpBadges()` | 在亮主方旁边显示主花色，持续到局末 |
| 必出牌高亮 | `ui.js` `renderHand()` | 跟对子/拖拉机时高亮必出牌 |
| 卡牌间距优化 | `css/style.css` | 选中/悬停时展开，减少遮挡 |

### 新增/修改方法索引

| 方法 | 文件 | 说明 |
|---|---|---|
| `_isEarlyGame(hand)` | ai.js | 判断开局阶段（手牌≥18） |
| `_isMidGame(hand)` | ai.js | 判断中盘阶段（手牌7~17） |
| `_shouldIntercept(...)` | ai.js | 判断是否应该拦截 |
| `_findInterceptCard(...)` | ai.js | 找副牌拦截牌（J/Q等） |
| `_findInterceptTrump(...)` | ai.js | 找主牌拦截牌（A/级牌等） |
| `_findBestSmallTrump(...)` | ai.js | 找小主牌（让队友上手策略） |
| `_hasTestedBigPairInAnySuit()` | ai.js | 判断是否出过大对子测试 |
| `_leadDealerVoidSuit(...)` | ai.js | 队友走庄家断门花色送牌权 |
| `_isSmallTrumpCard(...)` | ai.js | 判断主牌是否为小主牌 |
| `updateDeckPosition()` | ui.js | 底牌跟随庄家位置 |
| `updateTrumpBadges()` | ui.js | 亮主标记持久显示 |

## 已知问题与改进方向

### 已知限制

1. **甩牌杀牌的边界情况**：集成测试中偶发 AI 非法出牌（30局中约1次），主要出现在甩牌跟牌的复杂场景，`ui.js` 中的 `forceValidPlay` 函数作为兜底处理
2. **三拖拉机/四拖拉机**：如 445566 这类三连对，代码按拖拉机规则处理但未专门优化识别和跟牌逻辑（出现概率极低）
3. **响应式布局**：当前界面针对桌面端设计，移动端体验未优化
4. **无联机功能**：纯单机，三个 AI 由本地 `ai.js` 驱动

### 建议改进方向

1. **联机对战**：引入 WebSocket 后端，将 AI 决策替换为远程玩家
2. **移动端适配**：使用 CSS 媒体查询和触摸手势优化
3. **牌型识别增强**：补充三连对、四连对等极低概率牌型的专门处理
4. **AI 难度分级**：通过调整 `decidePlay` 中的策略权重实现简单/普通/困难
5. **回放功能**：利用 `playedCardsHistory` 实现对局回放
6. **甩牌提示**：玩家出牌时高亮可甩牌组合

## 技术栈说明

- **HTML5**：单页面，DOM 结构定义所有 UI 元素
- **CSS3**：Flexbox 布局，CSS 动画（发牌、出牌、翻牌），无 CSS 框架
- **Vanilla JavaScript**：ES6 class 语法，无框架、无打包工具、无依赖
- **全局变量通信**：`game.js` 定义的全局常量和函数被 `ai.js` 和 `ui.js` 直接引用

## 开发约定

- 所有注释使用中文
- 函数命名：公共函数用 camelCase（如 `getCardValue`），AI 内部方法用下划线前缀（如 `_leadPlay`）
- 常量全大写（如 `SUITS`、`RANKS`、`TEAMS`）
- 玩家位置统一使用 `bottom`（玩家）、`left`（上家）、`top`（对家）、`right`（下家）
- 队伍定义：`TEAM_A = [bottom, top]`（我方），`TEAM_B = [left, right]`（对方）
